{
  "name": "hdb-functions",
  "version": "1.0.0",
  "description": "JavaScript package for generating HarperDB requests",
  "files": [
    "lib/**/*"
  ],
  "main": "lib/Harper.js",
  "scripts": {
    "test": "jest --watchAll --verbose",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run format && npm run build",
    "start": "npm run format && npm run build"
  },
  "author": "Samuel Krohn",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.6"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node-fetch": "^2.5.10",
    "jest": "^27.3.1",
    "prettier": "^2.4.1",
    "ts-jest": "^27.0.7",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.5.2"
  }
}